<!-- COMMIT_STATUS START -->
> **커밋 상태**
> - 기준 커밋: `7b2ca723e428a472f503e11fdfe9d55977554560` (`main`)
> - 최근 커밋: `7b2ca723e428` Polish report persona voice
> - 커밋 일시: `2026-06-25T22:30:37+09:00`
> - 워킹트리: `dirty (1 files)`
> - 문서 갱신: `2026-06-25 23:02:32 +0900`
<!-- COMMIT_STATUS END -->

# 결정 기록

마지막 갱신일: 2026-06-20

## 2026-06-20 — 제품 정본을 생애 문제 컨설팅 루프로 갱신

**결정:** sajulife의 현재 제품 정본은 "사주/기질 기반 생애 문제 컨설팅 웹 서비스"다. 단순 사주 풀이 또는 프롬프트 튜닝 도구가 아니라 `리포트 -> AI 상담 -> 코칭 액션 -> 재방문` 루프를 검증한다.

**이유:**
- 상위 프로젝트 메모의 핵심 질문이 "사용자가 한 번의 컨설팅 후 다음 생애 문제로 재방문하는가?"다.
- 현재 구현도 이미 4종 리포트, 상담, 코칭, 공유, 계정 전환까지 포함한다.
- 프롬프트 튜닝은 운영 내부 기능이고, 사용자 가치의 중심은 상담과 실행 액션이다.

**문서 반영:**
- `docs/product/PRODUCT_BRIEF.md`
- `docs/product/MVP_SCOPE.md`
- `docs/product/USE_CASES.md`
- `docs/product/FEATURE_MAP.md`

## 2026-06-20 — Gemini 2.5 Pro를 기본 품질 기준으로 사용

**결정:** 기본 모델은 `GEMINI_MODEL || gemini-2.5-pro`다. 상담 근거 요약은 `GEMINI_SUMMARY_MODEL || gemini-2.5-flash`를 사용한다.

**이유:**
- 현재 프롬프트는 유료 심층 리포트 수준의 긴 한국어 출력과 구조화된 JSON/텍스트를 전제로 한다.
- 비용 0 검증은 `scripts/eval`과 Gem 복붙 플로우로 분리하고, 운영 품질 기준은 Pro에 맞춘다.
- 상담 근거 요약은 사용자 노출 본문이 아니므로 비용/속도를 위해 Flash 기본값이 적절하다.

**리스크:**
- Google AI Studio 무료 티어 키로는 Pro 호출이 불가능할 수 있다.
- 외부 베타/결제 전에는 입력 데이터 사용 조건과 paid tier 전환을 확인해야 한다.

## 2026-06-20 — 프롬프트 defaults.ts를 source of truth로 유지

**결정:** `lib/prompts/defaults.ts`가 프롬프트 원본이다. KV `prompts` override는 관리자 편집값으로 허용하되, default의 `version`이 더 높으면 default를 우선한다.

**이유:**
- 운영 중 관리자가 프롬프트를 조정할 수 있어야 한다.
- 동시에 코드에 포함된 검증된 기본 프롬프트가 낡은 KV 값에 영구히 가려지면 안 된다.
- 버전 bump로 옛 override를 무효화할 수 있어야 한다.

**현재 prompt key:**
- `tci-report`
- `personal-saju`
- `family-saju`
- `tci-saju-fusion`
- `consult`
- `consult-basis`

## 2026-06-20 — 공개 공유는 스냅샷 링크로 구현

**결정:** 리포트 공유는 인증이 필요한 원본 데이터를 직접 노출하지 않고, `share:{token}`에 자기완결 스냅샷을 저장해 `/share/{token}`에서 공개 렌더한다.

**이유:**
- 비로그인 사용자가 링크를 열 수 있어야 한다.
- 공유 시점의 시각화와 본문을 보존해야 한다.
- 원본 사용자 KV를 공개 페이지에서 직접 읽지 않게 격리한다.

**정책:**
- 같은 사용자/리포트 종류는 `user:{userId}:share:{kind}` 토큰을 재사용한다.
- 재공유 시 같은 링크의 스냅샷을 최신 내용으로 갱신한다.
- 현재 revoke UI는 없다. 외부 베타 전 revoke 또는 만료 정책을 결정해야 한다.

## 2026-06-20 — 상담은 리포트 원문이 아니라 요약 근거를 사용

**결정:** AI 상담은 저장된 리포트 원문 전체가 아니라 `user:{userId}:consult-basis`의 종류별 요약을 우선 사용한다. 요약이 없거나 낡았으면 상담 시 백필한다.

**이유:**
- 리포트 원문은 길어 상담 프롬프트 비용과 지연이 커진다.
- 사용자가 어떤 리포트를 근거로 쓸지 매번 고르게 하면 상담 진입이 느려진다.
- 존재하는 근거를 모두 합치는 기본값이 초기 UX에 적합하다.

**폴백:**
- 리포트가 하나도 없으면 상담 생성을 막고, 개인 사주 같은 리포트를 먼저 만들도록 안내한다.
- 리포트가 있는데 요약이 없거나 낡았으면 상담 시 백필한다.
- 요약 실패는 리포트 생성은 막지 않지만, 상담은 저장된 리포트 근거가 있어야 시작한다.

## 2026-06-20 — 리포트 액션 후보를 코칭 플랜으로 분리

**결정:** 리포트/상담이 제안하는 행동은 본문에만 남기지 않고 `SuggestedAction[]`으로 파싱해 코칭 플랜에 등록할 수 있게 한다.

**이유:**
- 리포트를 읽고 끝나는 문제를 줄여야 한다.
- "오늘/이번 주/이번 달" 실행 단위를 모아 재방문 루프를 만들 수 있다.
- 상담/리포트의 가치를 행동으로 확인할 수 있다.

**저장 정책:**
- 등록된 액션은 `user:{userId}:actions`에 최신순 저장한다.
- 최대 200개를 유지한다.
- 같은 `source + title`은 중복 등록하지 않는다.

## 2026-06-20 — 모든 AI 리포트의 톤을 반말 팩폭으로 통일

**결정:** 개인 사주, 기질, 융합, 가족, 상담 리포트는 "입 거친 친언니/형" 반말 팩폭 톤을 기본으로 한다. 단 가족 구성원 묘사는 존중을 유지한다.

**이유:**
- 현재 프롬프트와 UX가 프리미엄 리포트의 읽는 재미와 공유성을 강하게 전제로 한다.
- 존댓말 일반론보다 자기 이야기처럼 느끼는 몰입감을 우선한다.
- 가족 리포트는 타인을 향한 비난으로 읽히지 않게 예외가 필요하다.

**주의:**
- 순수 UI, 인증, 에러/검증 메시지는 존댓말을 유지한다.
- 공포 조장, 인신공격, 가족 구성원 비난은 금지한다.

## 2026-06-20 — TCI는 7차원 + AI 추정 유연성 축으로 표현

**결정:** 사용자 노출 TCI 라벨은 `추진성·안정성·공감성·지속성·주도성·연결성·통찰성` 7차원으로 쓰고, 유연성은 AI가 7차원 조합에서 추정하는 8번째 축으로 표시한다.

**이유:**
- 임상 용어를 그대로 노출하면 일반 사용자에게 딱딱하고 제품 톤과 맞지 않는다.
- 유연성은 현재 제품 경험에서 행동/상담으로 연결하기 좋은 보조 축이다.

**한계:**
- 유연성은 정식 TCI 측정값이 아니다.
- 정식 140문항은 라이선스 자료 입력과 권리 확인 전까지 실사용 금지다.

## 2026-05-29 — 이메일 회원가입/로그인 + 게스트→회원 연동

**결정:** Supabase Auth 위에 이메일/비밀번호 인증을 추가한다. 게스트(익명 세션)는 새 계정을 만드는 대신 기존 익명 계정에 이메일/비밀번호를 연동(`updateUser`)해 정식 회원으로 전환한다.

**이유:**
- 게스트 데이터는 `user.id`(auth.uid) 키로 KV에 저장된다.
- 익명 -> 정식 전환 시 `user.id`가 그대로 유지되어 별도 데이터 마이그레이션 없이 사주, 설문, 가족, 리포트가 보존된다.
- 새 계정을 만들고 데이터를 옮기는 방식은 충돌/유실 위험이 크다.

**구현:**
- `app/auth/login` — `signInWithPassword`.
- `app/auth/signup` — 세션이 익명이면 `updateUser({ email, password })`, 세션이 없으면 `signUp`.
- `app/auth/confirm/route.ts` — PKCE `code`와 OTP `token_hash` 모두 처리.
- `app/account` — 계정 상태, 게스트면 회원 전환 CTA, 로그아웃.
- 미들웨어는 `/auth/*`, `/share/*`, `/api/*`를 public으로 둔다.

**이메일 확인:** 프로토타입 단계에서는 OFF 운영 가능. 실제 결제 사용자 또는 외부 공개 시 커스텀 SMTP와 Confirm email ON을 다시 결정한다.

## 2026-05-26 — 초기 스택

**결정:** 웹 서버는 Next.js 15 App Router + TypeScript. 배포는 Vercel을 기본 가정한다. LLM provider abstraction은 Gemini부터 시작한다.

**이유:**
- App Router 하나로 프론트와 API endpoint를 같이 두면 thin-slice 사이클이 짧다.
- Vercel, Upstash KV, Supabase 조합이 초기 운영 부담을 낮춘다.
- 같은 스택 경험을 재사용할 수 있다.

## 2026-05-26 — Gemini 사용과 paid tier 전환 조건

**결정:** Explore 단계에서는 Gemini API로 시작한다. 초기에는 비용을 낮추되, 제품 품질 기준은 Pro 모델에 맞춘다.

**리스크:**
- 무료 티어 입력 데이터가 모델 개선에 사용될 수 있는지 확인이 필요하다.
- rate limit과 SLA가 외부 사용자에게 부족할 수 있다.

**전환 트리거:**
- 실제 결제 사용자가 발생할 때.
- 외부 베타 트래픽이 무료/저가 tier 한도에 닿을 때.
- 개인정보 처리방침에서 입력 데이터 보호를 명시해야 할 때.

## 2026-05-26 — 만세력 라이브러리 즉시 도입

**결정:** LLM에 사주 계산을 맡기지 않고 `lunar-javascript`로 천간·지지·일주·오행 분포·띠·대운을 결정론적으로 계산한다.

**이유:**
- LLM이 사주를 계산하면 같은 입력에도 결과가 흔들릴 수 있다.
- 프롬프트 튜닝과 제품 검증에서 계산 노이즈를 분리해야 한다.
- 만세력은 규칙 기반 계산이므로 라이브러리 사용이 자연스럽다.

**한계:**
- 윤달, 분 단위 시주 경계, 서머타임/표준시 보정은 prototype 수준이다.
- 정밀도가 제품 신뢰의 핵심 이탈 요인으로 확인되면 보정 로직을 추가한다.

## 2026-05-26 — AI 공급자 추상화

**결정:** `lib/ai/types.ts`의 `AIProvider` 인터페이스와 `lib/ai/index.ts`의 `getAIProvider()` 팩토리로 호출부를 격리한다.

**이유:** Gemini로 시작하지만 paid tier, 다른 Gemini 모델, 또는 다른 공급자로 바꿀 가능성이 있다.

## 2026-05-26 — 기질 검사는 자체 프로토타입 문항으로 시작

**결정:** 정식 TCI 문항은 저작권/라이선스 이슈가 있으므로, 프로토타입은 자체 35문항 약식판으로 시작한다. 정식 140문항 슬롯은 별도로 두되 운영팀이 권리 있는 자료를 입력하기 전까지 실사용하지 않는다.

**이유:** thin-slice 목적은 정밀 심리검사가 아니라 리포트/상담 루프의 가치 검증이다.

**전환 트리거:** 기질 측정 정확도가 제품 신뢰/결제에 직접 영향을 준다는 근거가 나오면 정식 TCI 라이선스 또는 다른 검증된 도구를 검토한다.
